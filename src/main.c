#include "app.h"


/**
 * @brief Entry point of the program.
 *
 * Calls the main application function.
 *
 * @return 0 on success, 1 on failure
 */
int main(void) {
    // while (1) {
    //     int result = run_app();
    //     if (result != 0) {
    //         return result; // Exit if the application returns a non-zero value
    //     }
    // }
    // return 0;
    return run_app();
}

